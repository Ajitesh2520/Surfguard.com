services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: surfguard
      POSTGRES_PASSWORD: surfguard
      POSTGRES_DB: surfguard
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
